---
# tasks file for snow-acm
- name: Create change request
  servicenow.itsm.change_request:
    instance:
      host: "{{ snow_credentials.instance | default(snow_instance, true)}}" 
      username: "{{ snow_credentials.username | default(snow_username, true)}}"
      password: "{{ snow_credentials.password | default(snow_password, true)}}"

    type: standard
    state: new
    requested_by: admin
    short_description: Install new Cisco Appliance
    description: Please install new Cat. 6500 in Data center 01
    priority: moderate
    risk: low
    impact: low

    other:
      expected_start: 2021-02-12
